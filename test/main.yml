---
- hosts: ubuntu
  become: true
  tasks:
    - name: install libselinux-python
      dnf:
        name: "libselinux-python"
        state: present
    - name: disable - selinux
      selinux:
        state: disabled

